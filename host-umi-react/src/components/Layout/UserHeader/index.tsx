export default () => {
  return (
    <div style={{
      height: 60,
      background: 'green',
      color: '#fff',
      padding: '0 20px',
      display: 'flex',
      alignItems: 'center',
    }}>
      👤 用户页头部（只在用户页显示）
    </div>
  );
};